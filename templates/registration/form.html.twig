{% extends 'base.html.twig' %}

{% block body %}
    <h1>Registration</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('app_login') }}" class="btn btn-secondary">Login</a>
{% endblock %}
